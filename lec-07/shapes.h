//shapes.h
//Header file and it should only contain function declarations

#include <string>
using namespace std;

void drawSquare(int n);
string drawRightTriangle(int n);
string drawTriangle(int n);
string drawD(int width, int height);
